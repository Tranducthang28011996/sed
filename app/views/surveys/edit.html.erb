<% title "Edit Survey" %>
<%= render 'surveys/links' %>

<br />
<nav class="short round">
  <ul>
    <% if can? :index, @survey %>
      <li><%= link_to "Show", @survey %> |</li>
    <% end %>
    <% if can? :index, @survey %>
      <li><%= link_to "View All", surveys_path %></li>
    <% end %>
  <ul>
</nav>
<div class="clear"></div>

<%= markdown "##Editing Survey: #{@survey.name}" %>
<%= render 'form' %>
